{
	"name": "opencode-comparator",
	"version": "0.1.0",
	"private": true,
	"description": "Compare coding models available through OpenCode Go, Zen and the free tier — pricing, context, capabilities and value for money.",
	"type": "module",
	"scripts": {
		"refresh": "node scripts/fetch-models.mjs",
		"serve": "node scripts/serve.mjs",
		"start": "npm run refresh && npm run serve",
		"pages:build": "node scripts/build-pages.mjs",
		"format": "prettier --write \"**/*.{md,html,css,mjs,js,json}\""
	},
	"engines": {
		"node": ">=20"
	},
	"devDependencies": {
		"prettier": "^3.9.6"
	}
}
